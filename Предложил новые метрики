**Дополнительное задание**
+   Выводы: в первые 3 дня резко падает посещаемость платформы (почти в 2 раза). 
+   Думаю, необходимо в первые дни увеличить начисления коинов за решения, задач, 
+   прохождения тестов (для того, чтобы пользователь адаптировался к платформе).
+   Предлагаю метрики, оценивающие действия пользователя в день регистрации и первые три дня для разработки в дальнейшем корректирующих мер:
+
+   - количество задач, решенных в эти дни (раздельно по дням попытки и решения):
+   Код решения (SQL):
+   a) попытки решения:
+
+   select diff, count(user_id) 
+   from(
+     select c.user_id, u.date_joined, c.created_at, (date(c.created_at) - date(u.date_joined)) as diff
+     from coderun c 
+     join users u 
+     on u.id = c.user_id 
+     where (date(c.created_at) - date(u.date_joined)) in (0, 1, 2)
+     order by c.user_id) as decision
+     group by diff
+
+   б) проверки
+
+   select diff, count(user_id) 
+   from(
+     select c.user_id, u.date_joined, c.created_at, (date(c.created_at) - date(u.date_joined)) as diff
+     from codesubmit c 
+     join users u 
+     on u.id = c.user_id 
+     where (date(c.created_at) - date(u.date_joined)) in (0, 1, 2)
+     order by c.user_id) as decision
+     group by diff
+
+   - количество тестов, пройденных в эти дни (раздельно по дням):
+   Код решения (SQL):
+
+   select diff, count(user_id) 
+   from(
+     select t.user_id, u.date_joined, t.created_at, (date(t.created_at) - date(u.date_joined)) as diff
+     from teststart t 
+     join users u 
+     on u.id = t.user_id 
+     where (date(t.created_at) - date(u.date_joined)) in (0, 1, 2)
+     order by t.user_id) as decision
+     group by diff
+
**Выводы:
+   Итоговые выводы по смене модели монетизации сервиса:
+   На мой взгляд необходимо продумать варианты различной монетизации для следующих групп:
+   - обучающиеся на курсах, которые решают необходимый перечень заданий – для них важны, в первую очередь задачи 
(платное решение – самое идеальное решение от преподавателя),
+   - пользователи, которые тренируются перед собеседованием (подписка на перечень тестов или группу задач с конкретным направлением),
+   - пользователи, которые в данный момент не хотят потерять определенные навыки или просто желающие повысить их уровень
(продолжительная подписка с платными идеальными решениями).

Дополнительное задание 2
Для выгрузки данных используется SQL-запрос:

select ...
Для хранения данных, структуры схемы, ..., используется такой код:

def foo(a, b):
    # не забудьте писать комментарии в коде, чтобы преподаватель понимал, что вы делаете
    ...
Выводы: Рекомендую проводить выпуски в такой-то день ..., в такое-то время ..., потому что ....
